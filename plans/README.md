# Implementation Plans

Generated by the improve skill on 2026-06-11 at commit `9c74569e` (branch
`kata-capture-bundle-link-semantics`): plans 001–006 from `/improve quick`
(correctness/security/tests audit), plans 007–009 from `/improve next`
(direction audit). Execute in the order below unless dependencies say
otherwise. Each executor: read the plan fully before starting, honor its STOP
conditions, and update your row when done.

Quick-audit scope notes: NOT audited were performance, tech debt, dependencies,
DX, docs, `tools/conformance/`, the render pipeline beyond existing tests, and
the uncommitted value-set-narrowing work in this worktree. Direction-audit
scope: all five direction findings are recorded here; two were deferred by the
maintainer (see "Direction findings deferred" below).

**Worktree warning for all executors**: this worktree carries unrelated
uncommitted work (modified `ehrs/epic/overlay.json`; untracked
`tools/analyze_value_set_narrowing.py`, `tests/test_analyze_value_set_narrowing.py`,
`reports/value-set-narrowing-epic-2026-05-15.md`). Never stage, revert, or edit
those files. Stage only your plan's in-scope files.

**Publication note for the maintainer**: plan 007 inlines strategy from the
intentionally-gitignored `PLAN.md` (long-tail vendor table, sequencing,
mock.health product split). `plans/` is NOT currently gitignored — decide
whether to add `plans/` to `.gitignore` or scrub 007 before any push.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Register Phase B vocabulary in overlay schema (restores green CI gate) | P1 | S | — | DONE |
| 002 | Enforce row_id recomputation in tools.validate; dedupe analyzer hash | P1 | S | 001 | TODO |
| 003 | Characterization tests for NPD + NPPES endpoint resolvers | P2 | M | — | TODO |
| 004 | Linkcheck: failed /metadata re-probe → transient, not broken | P2 | S | — | TODO |
| 005 | Token cache: create 0600 atomically, fail loud | P2 | S | — | TODO |
| 006 | Tests for ensure_row_id + backfill (golden hash anchor) | P3 | M | — | TODO |
| 007 | SPIKE: long-tail fleet ingest — provenance contract, validate relaxation, Athenahealth sampling spec | P1 | M | — | TODO |
| 008 | RUNBOOK: Cerner evidence parity — phases 3/4/6 (stage A credential-free, stage B needs CERNER_NONPROD_*) | P1 | M | 001 (recommended) | TODO |
| 009 | Pagination capture completion — empty-bundle shapes + Epic page caps (stage B needs sandbox creds) | P2 | S | 001 (recommended) | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 002 requires 001 because the full-suite verification in 002 expects a green
  baseline; before 001 lands, pytest has 2 pre-existing failures
  (`test_verified_via_in_schema_enum[meditech]`, `test_real_overlays_all_validate`)
  that would mask regressions.
- 003–006 are independent of each other; their plans name the two pre-001
  failures as the only tolerated ones.
- 007's stage outputs spawn follow-up build plans (schema change, Athenahealth
  ingest, eCW/healow ingest) — number them 010+ when written.
- 008 and 009 are staged: stage A is executable without credentials; stage B
  needs `.env` sandbox creds (gitignored — absent in fresh worktrees).
- Repo owner prefers one concern per PR — do not batch plans into a single PR.

## Direction findings deferred (maintainer's call, 2026-06-11)

- **Cross-vendor element comparison page** (`METHODOLOGY.md:186` TODO;
  `tools/render_html.py` pivot by (profile, path) across vendors) — deferred,
  not rejected. Pairs best with measurement-parity work (008) so the MEDITECH
  column isn't visibly skeletal.
- **Quarterly artifact parity** (no `meditech-conformance-matrix.json`, no
  cerner/meditech summary markdowns in `reports/2026-q2/`; README promises a
  `CHANGELOG.md` that doesn't exist) — deferred; sequence after 008-class
  measurement work so the summaries have substance.

## Findings considered and rejected

- Broad `except Exception → ""` in `tools/resolve_endpoints_to_npd.py:_hostname`
  (lines 49–53): benign — an unparseable URL correctly lands in "unresolved";
  urlparse exceptions are vanishingly rare on harvested URL strings.
- LLM output demotion in `tools/llm_disambiguate.py` (~line 244): by-design —
  the returned CCN is validated against the deterministic candidate list and
  explicitly flagged (`confidence="none"`, INVALIDATED reason) when it misses.
  Not a trust-boundary hole.
- Missing log context on JSON decode errors in `tools/measure_phase_b.py`
  (~lines 418, 458): logging nicety; fixtures are version-controlled so a
  malformed one is caught by other gates. Not worth a plan.
- `tools.validate` exit-code behavior: checked — it correctly exits 1 on errors
  (`tools/validate.py:414`). Not a finding.
- `verified_via` schema enum reserves `ecw_public_sandbox` /
  `veradigm_public_sandbox` / `nextgen_public_sandbox` with no `ehrs/` dirs:
  defensible roadmap markers; plan 007's long-tail work will consume or prune
  them — no standalone action.
- Tests for `tools/harvest_production_capstmts.py` error classification:
  deferred, not rejected — valuable, but needs HTTP-mocking infrastructure the
  repo doesn't have; revisit after 003 establishes resolver test patterns.
- `tests/test_methodology.py` promised at `METHODOLOGY.md:438` but absent:
  recorded for a future tests-category audit; not planned this round.
